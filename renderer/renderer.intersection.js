(function (mod) {
    //
    // constructor
    //
    function intersection(initial_distance, initial_object, initial_point) {
        this.distance = initial_distance;
        this.object = initial_object;
        this.point = initial_point;
    }

    //
    // class methods
    //
        
    //
    // instance methods
    //

    // todo?: toString

    mod.intersection = intersection;
}(mod || (mod = {})));